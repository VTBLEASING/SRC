create or replace procedure dm.analyze_table_test(p_table_name in varchar2,
                                            p_schema     in varchar2 default null) is
    c_proc constant varchar2(30) := 'analyze_table';
    l_current_schema varchar2(100);
  begin
  NULL;
  end;
/

